const IDENTITY_PRECOMPILE_BASE_COST: u8 = 15;
const IDENTITY_PRECOMPILE_COST_PER_WORD: u8 = 3;

mod IdentityPrecomile {
    fn exec_identity_precompile(input: Span<u8>, gas_limit: u64) -> (Array<u8>, u64) {
        let gas =
    }
}
